# weather.app.py
Weather App CLI

A simple Python command-line tool that fetches real-time weather data for any city using the OpenWeatherMap API.

## 🚀 Features
- Real-time temperature fetching.
- Weather condition descriptions (e.g., Clear sky, Rain).
- User-friendly CLI interface.

## 🛠️ Requirements
This project requires the `requests` library.
```bash
pip install requests